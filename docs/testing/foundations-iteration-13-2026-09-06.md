# Iteration 13: production integration evidence index

The frozen 829-file application build has passed production full/VCA2 API parity on the consumed qualification corpus. This is bounded implementation evidence, not error-free recognition, physical microphone qualification or a worldwide reliability claim.

## Accepted production parity

[Production decision and remaining errors](../../../foundations-evidence/iteration-13/application-integration/PRODUCTION-PARITY-DECISION.md) records the exact scope. [Primary acceptance](../../../foundations-evidence/iteration-13/application-integration/qualification-recovery/acceptance.json) SHA `642c668eb7eccd564bb52d546ca2b0a6636067d5b152e1f7a1c2e5be0a361784` reuses the completed original 115-input full stage under its original identities and validates all 115 new VCA2 inputs. Each mode has 189 ordered native decisions, 117 source mappings and 121 inherited reference gates; total mappings are 234. Text, coverage, receipts and semantic decisions match the accepted diagnostic and each other. Three existing optional-decode timing fields are excluded from semantic comparison.

The [original interrupted campaign](../../../foundations-evidence/iteration-13/application-integration/qualification/acceptance.json) remains failed. Its 38 observed VCA2 responses are retained; missing results were not inferred or accepted. The recovery campaign did not rerun full or select a subset of missing cases.

Base.en and the frozen decoder policy remain unchanged. Exactness/component nonregression and the original per-cohort WER limits remain enforced. Natural recognition errors remain: the consumed 16-recording set has 60 substitutions, 4 deletions and 10 insertions over 1,248 reference words in each production mode. The decision note lists other cohorts separately; overlapping logical references are not a new pooled benchmark.

## Package and current runtime scope

[Package manifest](../../../foundations-evidence/iteration-13/application-integration/package-build-01/PACKAGE-MANIFEST.json) binds package `d182bfe44ac2233435a5fe9b7040348d72ce473113baed073092528351797bb0` to build `0ee29ace94ec5916fdabea32200560103cda075fa3fb72a2acbc3335514fb0b5`. The exact Tauri UNK-to-DEB marker transform is retained separately; packaged GUI bytes are not claimed identical to unbundled GUI bytes.

The [first package runtime campaign](../../../foundations-evidence/iteration-13/application-integration/platform-package-01/EXECUTION.json) is **10/11, not accepted as a complete platform pass**: six X11 cases, three Chromium cases and nested GNOME pass; nested KDE failed its watcher-owner harness assertion before the intended journey. Preserve this campaign even if a corrected independently reviewed KDE harness later succeeds. A harness assertion is not itself proof of an application defect. Actual physical-device, installed-session and comparative dictation qualification remain open. The [separate KDE recovery execution](../../../foundations-evidence/iteration-13/application-integration/platform-kde-recovery-01/EXECUTION.json) reports success after correcting watcher startup validation to use the actual D-Bus owner and executable identity. The [independent outcome audit](../../../foundations-evidence/iteration-13/application-integration/audits/capture-kde-recovery-outcome-lock.json) verified package/model identity, the private bus owner, complete fixture alignment, Copy behavior and the reopened panel. Together, the retained first-run passes and this separate recovery cover all 11 distinct planned cases; they are not a single 11/11 campaign. This separate result does not erase or relabel the first 10/11 campaign.

## Architecture and error evidence

The [current streaming architecture](../cursor-streaming-dictation-hardening-spec.md) describes native receipt-driven ownership and the separate source-preprocessing cache. The [old manual QA](cursor-streaming-manual-qa.md) explicitly applies to historical IBus behavior, not this candidate; do not rewrite its old cadence as current instructions.

[Copied command-error renderer tests](../../../foundations-evidence/iteration-13/application-integration/audits/capture-command-error-renderer-execution-review.json) passed 52 scenarios against unchanged application source, including an early VCA2 string error, exact retained PCM and metadata, bounded Stop/manual retry, and no destination writes during recovery. This uses a mocked native boundary, not an injected real OS mutex failure. The tests are now integrated. Both new pure Rust `into_result` tests passed, covering complete/silent success and rejection of partial, missing or contradictory completion. The final `npm test` run passed its script suites, including four KDE identity checks, and 244 frontend tests; two pre-existing frontend tests remain skipped. [Validation logs](../../../foundations-evidence/iteration-13/application-integration/final-source-validation/) retain the exact results.

## Physical and comparator proposals

[Physical microphone protocol](physical-microphone-qualification.md) still requires explicit consent and a verified complete recording/reference. The [physical evaluator v2](../../../foundations-evidence/iteration-13/application-integration/physical-adapter-proposal/SOURCE-READY-v2.json) is now independently reviewed as a ready evidence proposal: complete full/VCA2 response and coverage checks, per-case and aggregate thresholds, silence handling, complete failure denominators, and consent-before-private-data validation are implemented. The [independent review](../../../foundations-evidence/iteration-13/application-integration/physical-adapter-proposal/DECODER-FINAL-REVIEW-v2.json) verified all 156 bindings and passed 10 Python and 19 JavaScript tests. The [source-bound Rust planner comparison](../../../foundations-evidence/iteration-13/application-integration/audits/capture-physical-planner-oracle-lock.json) covered 168 public/generated inputs: 164 valid inputs with 290 receipts and four rejected inputs. This bounded comparison does not prove every possible input. No actual physical recording, consent/session declaration or execution authorization is established by this readiness evidence. A transport-complete run alone cannot satisfy physical qualification.

The [completed six-case descriptive comparison](../../../foundations-evidence/iteration-13/application-integration/comparator-update-proposal/description.json), SHA `eb49a35e56b3d54b97d13bbb9ca44f983c58940d93299be0cc1a0d71a1f8222e`, retains 7 current-application edits (5S/1D/1I), 11 historical-VOCO edits (5S/1D/5I) and 12 Vibe edits (5S/1D/6I), each over the same 85 reference words. Only the continuity fragment differs: current0 versus historical4 and Vibe5 insertions; the other five cases have equal edit counts, including retained lexical errors. This reused six old peer results and five accepted current outputs, adding one complete Kirkleatham-clean application call with the same base.en model. It is a small consumed diagnostic comparison, not product ranking, matched timing, physical dictation evidence or new unseen quality. No model or download was added.

## Delivered source overlay

The historical frozen 831-file overlay at `final-source-delivery` adds two files and changes seven existing files relative to the original 829-file package input: regression tests, the KDE qualification harness, test wiring and documentation. At that historical freeze, production Rust code before `#[cfg(test)]`, the frontend application and decoder dependencies were unchanged, and the overlay had not been rebuilt. [The delivery manifest](../../../foundations-evidence/iteration-13/application-integration/final-source-delivery/DELIVERY-MANIFEST.json) preserves those two source states and their validation. The current working source additionally contains the later onboarding CSS correction, these two testing-document updates and the shortcut-reporting wording correction described below. The CSS correction was rebuilt into the separate package identified below. Recognition, capture and shortcut registration behavior are unchanged by the later wording correction; earlier package identities remain historical.

## Installed GNOME: ongoing preliminary findings

The isolated local KVM guest exposed an onboarding overflow defect at the standard 760×560 window: Hotkey and Continue were clipped and wheel scrolling was disabled. The one-line CSS correction restores the existing scroll container; [renderer evidence](../../../foundations-evidence/iteration-13/application-integration/onboarding-fix/REVIEW.json) covers wheel and keyboard access at 760×560 and 640×480. The [replacement package](../../../foundations-evidence/iteration-13/application-integration/package-onboarding-02/PACKAGE-MANIFEST.json) is SHA256 `7fa0e4b3a16ea8d33b352dc1e960320105d2a72eab8f345d70e7e49916fe78d5`, with GUI `42b70d3f0d879ed724e228d8caee1827668fd28bb61fa2f5e2e734883b0652b4`; the browser host is unchanged. Its 831-file source differs from the prior delivery only by this CSS line. Earlier packages, archives and failed receipts remain historical evidence.

The [first installed short-fixture audit](../../../foundations-evidence/iteration-13/application-integration/local-vm/short-installed-01/CAPTURE-INDEPENDENT-LOCK.json) observed zero substitutions, deletions and insertions, complete prepared-audio coverage, and whole-fixture waveform correlation 0.996856. However, requested-to-transcript-ready time was **48.852 seconds against the frozen 45-second deadline**, so the overall case failed. These are virtual audio fixture findings, not physical microphone results. The hotkey was unavailable in this guest journey. Device discovery worked in a later app process, but package, onboarding state and elapsed time also changed; that sequence does not isolate a restart as the remedy. The initial Copy/Open hiccup has no confirmed cause. A [separate second Copy action](../../../foundations-evidence/iteration-13/application-integration/local-vm/short-installed-01/DECODER-COPY-ADDENDUM.json) verified exact clipboard text and does not erase the original failure. Installed-session qualification remains ongoing; none of these preliminary observations establishes a complete case campaign pass or a worldwide reliability claim.


## Installed reboot and raw capture localization

The [separate reboot case](../../../foundations-evidence/iteration-13/application-integration/local-vm/run-02/REBOOT-CASE-ACCEPTED.json) passed with the onboarding-corrected package: same guest machine, new boot and GDM session, exact GUI/browser-host/model identities and prior configuration, active PipeWire/IBus/portal services, tray launch and Quit, and exact manual Copy. The short public recording completed in 10.964 seconds against its 45-second deadline, with zero errors over four reference words, waveform correlation 0.993118 and minimum active-quarter correlation 0.989581. This validates one installed virtual-guest reboot case. It does not erase the earlier timed-out short case, prove physical-device persistence or qualify the unavailable hotkey. The guest retained its networkd configuration and address but had no IPv4 default route; this offline test used the pinned SSH connection and local fixture.

The [installed repeated recording](../../../foundations-evidence/iteration-13/application-integration/local-vm/repeat-installed-coordinated-01/CAPTURE-INDEPENDENT-LOCK.json) recognized all 16 phrases/64 words with complete canonical receipts, yet **failed waveform continuity**. A [simultaneous source-mirror diagnostic](../../../foundations-evidence/iteration-13/application-integration/local-vm/mirror-diagnostic-01/independent/INDEPENDENT-LOCK.json) found the direct 16 kHz mono Pulse recording continuous while VOCO's prepared recording contained discrete timing shifts. Exact text alone did not satisfy the audio gate.

A separate `foundations-audio-diagnostic` checkout adds an opt-in pre-resampling source export, enabled only with both `VOCO_DEBUG_CAPTURE_AUDIO=1` and `VOCO_DEBUG_RAW_SOURCE=1`. The [834-file diagnostic package](../../../foundations-evidence/iteration-13/application-integration/raw-ingress-diagnostic/package-01/PACKAGE-MANIFEST.json), SHA256 `30efbea03a7d554f518de4998c88f9acfb58f32515d1e03dd839ce3bd8f45724`, changes three existing files and adds three helpers/tests relative to the 831-file onboarding package; the other 828 files remain exact. Capture callbacks, worklet, resampling and recognition policy are unchanged. Sixteen focused frontend tests, type checking, frontend build, two new Rust tests and the existing private-pair test passed. Independent review caught and corrected a missing native function signature before packaging; the original source receipt and first missing-frontend-dist compile failure remain retained. The diagnostic package passed build, lint, version checks and Debian verification and was installed only in the virtual guest.

The [raw-source diagnostic](../../../foundations-evidence/iteration-13/application-integration/raw-ingress-diagnostic/analysis/run-01/INDEPENDENT-LOCK.json) retained 1,827,968 source samples at 44.1 kHz with 893 contiguous delivered chunks and successful worklet-flush acknowledgment. Two independent analyses found the same two nonzero, bit-exact adjacent 128-frame repeated blocks before DC removal or resampling. The simultaneous Pulse mirror passed the unchanged waveform gate at 0.999986, while VOCO's prepared WAV failed at 0.744391. Across 96 checked windows, prepared audio matched the raw source with zero relative lag and minimum correlation 0.999944. This localizes those repeated blocks upstream of application preprocessing. Late alignment to the original fixture exceeded the frozen diagnostic search range and remains unresolved; no search widening or input repair was used. All 64 words and two canonical receipts remained exact, which does not turn the failed waveform into a pass.

This diagnostic also retains a [bookkeeping preflight failure](../../../foundations-evidence/iteration-13/application-integration/local-vm/raw-mirror-diagnostic-01/PREFLIGHT-BOOKKEEPING-FAILURE.json): an evidence filename was wrong, and the shell still launched the separately reviewed recording wrapper. The method had already been registered, but input binding was completed after capture. This run is diagnostic evidence, not a clean qualification campaign. No automatic retry was used.

The [versioned WebKit caller review](../../../foundations-evidence/iteration-13/application-integration/raw-ingress-diagnostic/analysis/webkit-2.52.6/REVIEW-LOCK.json) found a plausible unchanged-buffer return on adapter-lock contention. The actual runtime lock event has not been observed, and the clean Pulse mirror requested a different format and buffering from WebKit. A [native capture comparison proposal](../../../foundations-evidence/iteration-13/application-integration/native/native-capture-boundary-proposal/PLAN.md) addresses that confound with the same source and observed 44.1 kHz stereo format. It is not an approved production capture replacement: device identity, interruption handling, stop completeness and the native permission/first-use behavior require evidence and an explicit product decision. Physical microphone, broad Linux and comparative product qualification remain open.

## Matched native capture diagnostic

The [prospective comparison plan](../../../foundations-evidence/iteration-13/application-integration/local-vm/NATIVE-COMPARISON-01-PLAN.json), SHA `7dada40b0586fbadd999f208200e34971a8b0dbdc564b45949571573ba735b85`, bound 50 local and 10 guest files before one recording. An initial plan-generation attempt referenced the preserved original analysis manifest and stopped before launch; the final plan explicitly binds reviewed `SOURCE-READY-v3.json`. The [outer result](../../../foundations-evidence/iteration-13/application-integration/local-vm/native-comparison-01/RESULTS.json) retains one coordinator invocation, zero retries, both successful process exits and complete source artifacts. The native subscriber stopped on the app's identity-bound capture-stop event, before subsequent recognition processing.

The [probe](../../../foundations-evidence/iteration-13/application-integration/native/native-capture-probe/SOURCE-READY.json) uses authenticated matching libpulse headers, a bounded callback queue and writer, explicit source identity, and acknowledged start/stop. Strict compilation and sanitizer tests passed after review fixed teardown, final-queue ordering, byte-accounting and error-report persistence. It ran only in the owned virtual guest. Actual concurrent Pulse streams had the same source, S16LE stereo at 44.1 kHz, maximum buffer 35,280 bytes and fragment 1,764 bytes. The source graph itself remained mono 48 kHz. This removes the earlier differing-client-format confound.

The [frozen analysis](../../../foundations-evidence/iteration-13/application-integration/raw-ingress-diagnostic/matched-native-analysis/run-01/INDEPENDENT-LOCK.json), SHA `4e8d86238343f941622f99ebf8faaaed570458c4dece06996e114d21e3880e43`, found:

| Measurement | Native subscriber | VOCO source before preprocessing |
| --- | --- | --- |
| Delivered frames at 44.1 kHz | 1,980,090 in 4,490 blocks | 1,827,328 in 893 chunks |
| Full-reference diagnostic correlation | 0.999847 in both channels | 0.356305 |
| Lag across 64 nonzero-reference windows | Constant in both channels | 512-sample span, about 11.61 ms |
| Nonzero exact adjacent 128-frame repetitions | None at the scanned block alignment | One, at source sample 1,680,512 |

Different total extents reflect different start/stop times; they are not a loss calculation. All 96 fixed windows remain retained, including 32 silent reference windows that cannot establish lag. The search range was not widened. The 512-sample span is not a count of proven duplicated blocks. Native absence at the scanned alignment does not rule out every possible phase-shifted duplicate.

The [independent capture audit](../../../foundations-evidence/iteration-13/application-integration/raw-ingress-diagnostic/analysis/native-matched-01/INDEPENDENT-LOCK.json), SHA `a582d67a29805dea2787bbe6a77c5135b37c9dda7b776d99ae67e7cc46fca3d4`, verified matching stream identities, every native byte/block and the same nonzero app repetition. VOCO's prepared audio **still fails the unchanged waveform gate**, at 0.355897 whole-reference correlation. Across that audit's separate 96 preprocessing-consistency windows, prepared audio follows the raw source at zero relative lag with minimum correlation 0.999958. This narrows the fault to the WebKit/app capture branch or its per-client path; a specific provider-lock failure remains unproved. The native receipt accounts for locally delivered audio, not all physical-device frames through a click.

The app nevertheless produced and copied all 16 phrases/64 words exactly, reaching idle in 52.722 seconds against 105 seconds. That semantic result does not override the failed waveform gate. The [unapplied native-backend design](../../../foundations-evidence/iteration-13/application-integration/native/native-capture-backend-design/DESIGN.md) preserves existing recognition and identifies native permission, device migration and activation decisions. A source descriptor is a future integration prerequisite, not a proven defect in the current WebKit context lifecycle. No native backend has been activated in production.

The guest's owned audio modules were removed, its prior virtual default source and 300-second idle timeout restored, and its [final evidence archive](../../../foundations-evidence/iteration-13/application-integration/local-vm/run-02/FINAL-ARCHIVE.json) verified. KVM exited normally with code zero and no supervisor signals. Physical-microphone consent and broader desktop/product qualification remain open.

## Truthful shortcut reporting

The installed guest revealed an independent reporting defect: discovery supervision and frontend event subscriptions were described as an attached hotkey listener even when no evdev keyboard was available. The [wording correction](../../../foundations-evidence/iteration-13/application-integration/native/shortcut-status-review/wording-change/SOURCE-READY.json) changes those messages to describe selection, discovery and subscriptions. Saving a preference no longer promises that the OS will deliver it. Existing trace event names, device access, shortcut registration and product behavior are preserved. Sixteen existing Rust hotkey tests, the frontend readiness test, type checking, focused lint and Rust formatting passed. This corrects success claims; it does not make the guest's unavailable Wayland shortcut work.

## Microphone recovery and preview startup

The [microphone recovery evidence](microphone-recovery.md) separates the installed baseline, the first candidate's retained meter failure and the second candidate's successful virtual-guest recovery trial. The frontend orders refresh/access results, keeps optional permission queries from blocking Settings, rejects stale Retry completions and restarts only the preview that still owns a successful Retry. Preview startup resumes its AudioContext and cleanup releases each owned stream/context once, including late resume completion.

Package `e299f193b324d9a576d547efc8fba17ee07d2d990ed44542f1e1c0af652b7246` passed source/assets/Debian verification and the planned one-source, one-Retry, one-playback guest journey: the old error cleared, the live meter visibly responded, and hiding Settings closed capture in the same GUI/WebKit processes. Cleanup and normal VM exit were verified. The final code passed 26 dedicated App renderer cases, the surrounding dictation renderer and the full frontend suite (253 passed, two existing skips). Both CI workflows now include the dedicated renderer check; no remote CI execution is claimed.

This package contains the earlier shortcut-reporting correction and leaves the current speech model, recognition policy and capture constraints unchanged. It was installed only in the local virtual guest. Its 835-file source snapshot precedes these final result notes; the current overlay adds documentation only. The separate waveform-continuity failure, physical microphone consent/testing, unavailable guest hotkey and broader Linux/comparative qualification remain open. This bounded recovery result does not complete the overall quality goal.
